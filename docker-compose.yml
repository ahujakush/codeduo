version: '3.8'

services:
  web-app:
    build: .
    container_name: web_ai_solver
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env
